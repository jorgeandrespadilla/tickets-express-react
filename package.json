{
  "scripts": {
    "start": "concurrently --kill-others npm:start:*",
    "start:client": "cd ./client && npm start",
    "start:server": "cd ./server && npm start",
    "prepare": "npm install && cd ./client && npm install && npm run build && cd ../server && npm install"
  },
  "dependencies": {
    "concurrently": "^7.4.0"
  }
}
